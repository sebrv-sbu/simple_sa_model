Currently very bare bones. If you want to use it, clone it into a directory, Run:

Cargo build --release

in that directory and then run it manually:

/PATH/TO/SIMPLE_SA_MODEL/target/release/simple_sa_models --help

See examples for an example input.
